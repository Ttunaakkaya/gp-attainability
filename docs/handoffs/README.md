# Historical handoff inputs — not active instructions

The active scope/order/completion criteria are [masterplan v2.0](../../GP_Attainability_Masterplan_TR.md)
and the current [priority document](../../AKTIF_ONCELIK_PROJE.md).

`2026-09-08/` is an immutable historical transfer package. Its older file named
`AKTIF_ONCELIK_PROJE.md`, transfer prompt, proposal/email drafts and scope decisions
do **not** override the active root documents. The complete package is retained because
`transfer_manifest.json` spans seven source documents and sixteen diagnostic files;
removing individual stale prompts would break that provenance set.

The original README's `GP_Attainability_Diagnostic/README_TR.md` link refers to the
source conversation's layout and was already unresolved after transfer. Its correct
project destination is [the preserved diagnostic](../../experiments/exact_gp_diagnostic/README_TR.md).
The packaged documents are intentionally not rewritten to fix historical links.
The transfer manifest uses paths relative to the project root.

The diagnostic's exact-GP, covariance, prefix and feasibility checks remain relevant
to v2 validation. Its equal/negative findings and all original data remain untouched.
