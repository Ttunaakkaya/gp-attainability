# Shell/runner template only; the application does not auto-load this file.
# Repository-relative output locations live in the resolved YAML configuration.

# Keep BLAS single-threaded inside process-level experiment parallelism.
OMP_NUM_THREADS=1
MKL_NUM_THREADS=1
OPENBLAS_NUM_THREADS=1
