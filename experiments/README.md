# Experiment roles

The [v2.0 masterplan](../GP_Attainability_Masterplan_TR.md) is authoritative.

- `exact_gp_diagnostic/`: preserved, hash-verified independent numerical diagnostic;
  a small-problem validation reference, not the completed main project. Do not delete
  unfavorable results or rewrite its original generated data.
- `manifests/final_protocol.yaml`: inactive, unfilled v1 freeze checklist retained
  because generic provenance fields remain useful. Its calibration field does not
  impose an active v2 requirement. No final protocol or held-out seeds are frozen.

The obsolete v1 `locked_splits.yaml` reservation was archived. Its namespace counts
are not active experiment assignments. New evaluation must follow the v2 freeze and
paired-task protocol, keeping seeds 7, 19 and 31 as already seen development evidence.
